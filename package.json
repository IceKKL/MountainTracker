{
  "name": "mountain-tracker",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix shared && npm run build --prefix shared && npm install --prefix client && npm install --prefix server",
    "dev": "npm run build --prefix shared && npm run dev --prefix server",
    "build": "npm run build --prefix shared && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
